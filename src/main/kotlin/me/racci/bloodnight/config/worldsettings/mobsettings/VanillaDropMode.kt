package me.racci.bloodnight.config.worldsettings.mobsettings

enum class VanillaDropMode {
    /**
     * Drop only vanilla drops
     */
    VANILLA,

    /**
     * Combine custom and vanilla drops
     */
    COMBINE,

    /**
     * Drop only custom drops
     */
    CUSTOM
}